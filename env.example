# Django核心配置
# 生产环境请使用强随机密钥，可通过以下命令生成：python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=ZfPW88lhCZMQA1M9IsmAy5GZMzNUuxxHEsA9i92Z2rXJPk8bXZHNfKg2ykgW0i_3XdE
# 生产环境必须设置为False
DJANGO_DEBUG=False
# 允许访问的主机，包含服务器IP、域名等（逗号分隔，不要使用*）
DJANGO_ALLOWED_HOSTS=101.42.172.229,localhost,127.0.0.1

# MySQL数据库配置
MYSQL_DATABASE_NAME=onlinejudge
# 如果MySQL在云服务器上，使用localhost（同服务器）或127.0.0.1
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=ueboj123

# JWT认证配置
JWT_ALGORITHM=HS256
JWT_EXP_DELTA_SECONDS=604800

# MinIO/S3对象存储配置
AWS_ACCESS_KEY_ID=minioadmin
# 生产环境请修改为强密码
AWS_SECRET_ACCESS_KEY=minioadmin
# MinIO服务地址，如果MinIO在云服务器上，使用localhost:9000（同服务器）或127.0.0.1:9000
AWS_S3_ENDPOINT_URL=http://localhost:9000
AWS_STORAGE_BUCKET_NAME=onlinejudge
# 如果使用HTTPS，设置为True
AWS_S3_USE_SSL=False
# 如果使用自签名证书，设置为False
AWS_S3_VERIFY=False
AWS_S3_FILE_OVERWRITE=True
AWS_QUERYSTRING_AUTH=False

